# ─── HuggingFace ──────────────────────────────────────────────────────────────
# (Optional) Access token — only needed for gated models.
# The default model (onnx-community/whisper-small) is open and needs no token.
# VITE_HF_TOKEN=hf_your_token_here
